name: CI feature-dev solo compilación

on:
  push:
    branches: [ "feature-dev" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'
      - run: mvn -B clean verify
