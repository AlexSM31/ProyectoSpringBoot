name: CD - Secretos de BD demo

on:
  workflow_dispatch:

jobs:
  secrets-demo:
    runs-on: ubuntu-latest
    env:
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
    steps:
      - run: |
          echo "Usaríamos la variable DB_PASSWORD para conectar a la BD en despliegue."
          echo "El valor real viene de GitHub Secrets y no aparece en el código."
